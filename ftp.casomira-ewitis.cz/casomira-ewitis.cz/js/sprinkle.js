$(document).ready(function() {
	//$tabs = $('#tabvanilla > ul').tabs({ fx: { height: 'toggle', opacity: 'toggle' } });
	//$tabs = $('#featuredvid > ul').tabs();
        var $tabs = $('.tabnav').tabs(); // first tab selected
        $tabs.tabs('select', 0);
});
